package dungeonmania.staticEntities;

import dungeonmania.util.Position;

public class Exit extends StaticEntity{
    public Exit (String id, Position position, String type) {
        super(id, position, type);
    }
}
